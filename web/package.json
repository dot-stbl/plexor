{
  "name": "plexor-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "tooling/*", "packages/*"],
  "scripts": {
    "dev": "bun --filter '@plexor/console' dev",
    "build": "bun --filter '@plexor/console' build",
    "test": "bun --filter '@plexor/console' test",
    "lint": "bun --filter '@plexor/console' lint",
    "typecheck": "bun --filter '@plexor/console' typecheck",
    "dev:docs": "bun --filter '@plexor/docs' dev",
    "build:docs": "bun --filter '@plexor/docs' build",
    "test:docs": "bun --filter '@plexor/docs' test",
    "lint:docs": "bun --filter '@plexor/docs' lint",
    "typecheck:docs": "bun --filter '@plexor/docs' typecheck"
  },
  "dependencies": {
    "@tanstack/react-table": "^8.21.3",
    "react-aria-components": "^1.21.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "playwright": "^1.63.0"
  }
}
