---
title: "Authentication"
icon: "KeyRound"
description: "OIDC flow через Keycloak, JWT claims, RBAC permissions."
---

TODO: страница в работе.

- Keycloak как внешний IdP (или local mode для dev/MVP)
- OIDC code flow с PKCE
- Структура JWT: `sub`, `org`, `team`, `folder`, `role`, `permission`, `service`, `iss=plexor`
- Permission hierarchy: `compute.vms.create` → `compute.vms.create.bulk` → `compute.vms.*` → `*`
- Tenant-scoped claims и global query filters