---
title: "Upgrade & Uninstall"
icon: "Terminal"
description: "Lifecycle app instance: upgrade до новой версии, uninstall."
---

TODO: страница в работе.

- `plx provider upgrade <instance-id> --to-version 0.3.0`
- State: `running → upgrading → running` (или `failed`)
- `DELETE /api/v1/marketplace/instances/{id}` — uninstall, async 202
- `GET .../logs?tail=200` — логи install/upgrade