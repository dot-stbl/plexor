---
title: "Authoring a provider"
icon: "Wrench"
description: "Как создать свой app provider."
---

TODO: страница в работе.

- Структура репозитория: `provider.yaml` + assets
- Schema: `apiVersion`, `kind`, `metadata`, `spec.resources`, `spec.config`, `spec.dependencies`, `spec.install`, `spec.upgrade`, `spec.uninstall`, `spec.healthCheck`
- Переменные: `$instanceId`, `$version`, `$port`, user-defined
- Публикация: git push / `podman push` / tarball