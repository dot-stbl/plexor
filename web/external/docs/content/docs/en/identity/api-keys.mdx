---
title: "API Keys"
icon: "ShieldCheck"
description: "API keys for service accounts and programmatic access."
---

TODO: page in progress.

- `ApiKey { Id, TenantId, UserId, HashedSecret, ExpiresAt }`
- `POST /api/v1/users/{id}/api-keys` — plaintext secret returned **once**
- Only the hash is stored