---
title: "Users & Roles"
icon: "KeyRound"
description: "User lifecycle, Role and RoleBinding, RBAC permissions."
---

TODO: page in progress.

- `User { Id, TenantId, Email, DisplayName, Status }`
- `Role { Id, TenantId, Name, Permissions }`
- `RoleBinding { Id, UserId, RoleId, ProjectId? }`
- API: `POST /api/v1/users`, `POST /api/v1/roles`, `POST /api/v1/role-bindings`