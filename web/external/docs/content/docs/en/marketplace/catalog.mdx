---
title: "Catalog"
icon: "MenuBook"
description: "Reference app providers from the Plexor team."
---

TODO: page in progress.

Reference providers:

- `postgresql` — Standalone Postgres 15+
- `redis` — Standalone Redis 7+
- `nginx` — Reverse proxy / ingress
- `minio` — S3-compatible object storage
- `keycloak` — OAuth/OIDC server
- `wordpress` — Popular CMS
- `ghost` — Modern CMS