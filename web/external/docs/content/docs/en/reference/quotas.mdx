---
title: "Quotas"
icon: "ShieldCheck"
description: "Per-tenant and per-project quotas."
---

TODO: page in progress.

Tenant-level: `max_cpus`, `max_ram_mb`, `max_volumes`, `max_buckets`.
Project-level — same fields, scoped to one project.
Exceeding returns `403 Forbidden` with `code: "compute.vms.quota"`.