---
title: "Authentication"
icon: "KeyRound"
description: "OIDC flow via Keycloak, JWT claims, RBAC permissions."
---

TODO: page in progress.

- Keycloak as external IdP (or local mode for dev/MVP)
- OIDC code flow with PKCE
- JWT structure: `sub`, `org`, `team`, `folder`, `role`, `permission`, `service`, `iss=plexor`
- Permission hierarchy: `compute.vms.create` → `compute.vms.create.bulk` → `compute.vms.*` → `*`