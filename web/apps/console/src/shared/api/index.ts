// Public barrel for the API layer.
// Generated code lives in ./src (kubb, clean:true — do not hand-edit).
// ./auth.ts is the handwritten surface for endpoints not yet in the
// contract (see its TODO(contract) header).
// Import from '@/shared/api' across the app so the generated path stays internal.
export * from './src';
export * from './auth';
