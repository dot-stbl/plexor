/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.

/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { AuthLoginResponse } from "../types/AuthLoginResponse.ts";
import { faker } from "@faker-js/faker";
import { createPostAuthLoginUser } from "./createPostAuthLoginUser.ts";

/**
 * @description Successful sign-in. Returns the bearer access token (RS256 JWT the console stores as `Authorization: Bearer`), the opaque refresh token, the access-token lifetime in seconds, and the authenticated user record. The FE computes `expiresAt = Date.now() + expiresIn * 1000` and persists the triple to localStorage.\n
 */
export function createAuthLoginResponse(data?: Partial<AuthLoginResponse>): AuthLoginResponse {

  return { ...{"accessToken": faker.string.alpha(),"refreshToken": faker.string.alpha(),"expiresIn": faker.number.int({ min: 1 }),get "user"() { return createPostAuthLoginUser() },...(data || {})} }
}