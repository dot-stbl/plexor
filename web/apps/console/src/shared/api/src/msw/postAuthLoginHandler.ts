/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.
//
// Hand-mirrored from contracts/plexor.openapi.yaml: kubb 4.39.2 silently
// skipped /auth/login in the MSW + faker pass — the endpoint is documented
// in Phase 4.6 (sigil: local credentials login), but the FE needed it
// before kubb caught up. This file restores parity so the dev:mock worker
// covers the login endpoint the console FE actually hits. If a future
// regen starts emitting these, delete this file and the kubb output will
// take over.
//
// Edit history:
//   - 2026-09-19: Default the response to a kubb-shaped faker fixture when
//     `data` is omitted. The original kubb stub
//     (`JSON.stringify(data)` with `data === undefined`) wrote an empty
//     body, which the FE's postAuthLogin() couldn't parse — login errors
//     were silent. See fix/fe/mocks: postAuthLogin handler returns real
//     mock response.

/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { PostAuthLoginMutationRequest, PostAuthLoginMutationResponse } from "../types/PostAuthLogin.ts";
import { http } from "msw";
import { createPostAuthLogin200 } from "../fixtures/createPostAuthLogin.ts";

export function postAuthLoginHandlerResponse200(data: PostAuthLoginMutationResponse) {
  return new Response(JSON.stringify(data), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
    },
  })
}

export function postAuthLoginHandlerResponse401(data?: PostAuthLoginMutationResponse) {
  return new Response(JSON.stringify(data), {
    status: 401,
    headers: {
      'Content-Type': 'application/problem+json',
    },
  })
}

export function postAuthLoginHandler(
  data?: PostAuthLoginMutationResponse | ((
    info: Parameters<Parameters<typeof http.post>[1]>[0],
  ) => Response | Promise<Response>),
) {
  return http.post(`/auth/login`, function handler(info) {
    if (typeof data === 'function') return data(info)
    // Default to a kubb-shaped faker fixture when no data is supplied —
    // the original kubb stub silently JSON.stringified `undefined`, leaving
    // an empty body that postAuthLogin() couldn't parse on the FE.
    return postAuthLoginHandlerResponse200(
      data ?? createPostAuthLogin200()
    );
  })
}

void {} as PostAuthLoginMutationRequest;
