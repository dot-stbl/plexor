/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.
//
// Hand-mirrored from contracts/plexor.openapi.yaml: see kz note in
// msw/postAuthLoginHandler.ts.

/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { problemDetailsSchema } from "./problemDetailsSchema.ts";
import { z } from "zod/v4";

export const postAuthLoginMutationRequestSchema = z.object({
    email: z.string().min(1).max(254).describe("Work email."),
    password: z.string().min(1).max(128).describe("Password."),
});

/**
 * @description Successful login — tokens + user record.
 */
export const postAuthLogin200Schema = z.object({
    accessToken: z.string().describe("JWT (RS256) the console stores and sends as `Authorization: Bearer`."),
    refreshToken: z.string().describe("Opaque refresh token (rotated on every refresh)."),
    expiresIn: z.int().describe("Access-token lifetime in seconds."),
    user: z.object({
        id: z.uuid(),
        email: z.string(),
        displayName: z.string(),
        roles: z.array(z.string()),
    }),
});

/**
 * @description Invalid credentials.
 */
export const postAuthLogin401Schema = z.lazy(() => problemDetailsSchema).describe("RFC 7807 problem details.");

/**
 * @description RFC 7807 error.
 */
export const postAuthLoginErrorSchema = z.lazy(() => problemDetailsSchema).describe("RFC 7807 problem details.");

export const postAuthLoginMutationResponseSchema = z.lazy(() => postAuthLogin200Schema);
