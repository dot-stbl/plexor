/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.

 import { problemDetailsSchema } from "./problemDetailsSchema.ts";
import { vmListSchema } from "./vmListSchema.ts";
import { vmStatusSchema } from "./vmStatusSchema.ts";
import { z } from "zod";

 export const listVmsQueryParamsSchema = z.object({ "zone": z.string().optional(), "status": z.lazy(() => vmStatusSchema).optional(), "q": z.string().describe("Free-text filter over name / IP / tags.").optional(), "page": z.number().int().min(1).default(1), "pageSize": z.number().int().min(1).max(100).default(20) }).optional();

 /**
 * @description Paginated list of virtual machines.
 */
export const listVms200Schema = z.lazy(() => vmListSchema);

 /**
 * @description RFC 7807 error.
 */
export const listVmsErrorSchema = z.lazy(() => problemDetailsSchema);

 export const listVmsQueryResponseSchema = z.lazy(() => listVms200Schema);