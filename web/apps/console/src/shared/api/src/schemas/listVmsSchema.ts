/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.

/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { problemDetailsSchema } from "./problemDetailsSchema.ts";
import { vmListSchema } from "./vmListSchema.ts";
import { vmStatusSchema } from "./vmStatusSchema.ts";
import { z } from "zod/v4";

export const listVmsQueryParamsSchema = z.object({
    "zone": z.optional(z.string()),
get "status"(){
                return vmStatusSchema.describe("Lifecycle state of a VM.").optional()
              },
"q": z.optional(z.string().describe("Free-text filter over name / IP / tags.")),
"page": z.coerce.number().int().min(1).default(1),
"pageSize": z.coerce.number().int().min(1).max(100).default(20)
    })

/**
 * @description Paginated list of virtual machines.
 */
export const listVms200Schema = z.lazy(() => vmListSchema)

/**
 * @description RFC 7807 error.
 */
export const listVmsErrorSchema = z.lazy(() => problemDetailsSchema).describe("RFC 7807 problem details.")

export const listVmsQueryResponseSchema = z.lazy(() => listVms200Schema)