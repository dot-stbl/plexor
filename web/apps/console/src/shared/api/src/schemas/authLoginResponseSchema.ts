/* eslint-disable */
// @ts-nocheck
// Generated by kubb — do not edit. Run `bun run generate` from web/tooling/codegen.

/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { postAuthLoginUserSchema } from "./postAuthLoginUserSchema.ts";
import { z } from "zod/v4";

/**
 * @description Successful sign-in. Returns the bearer access token (RS256 JWT the console stores as `Authorization: Bearer`), the opaque refresh token, the access-token lifetime in seconds, and the authenticated user record. The FE computes `expiresAt = Date.now() + expiresIn * 1000` and persists the triple to localStorage.\n
 */
export const authLoginResponseSchema = z.object({
    "accessToken": z.string().describe("RS256 JWT. Sent as `Authorization: Bearer ` followed by this token on subsequent requests.\n"),
"refreshToken": z.string().describe("Opaque refresh token. Rotated on every successful refresh."),
"expiresIn": z.int().min(1).describe("Access-token lifetime in seconds."),
get "user"(){
                return postAuthLoginUserSchema.describe("The authenticated user record — used to seed the FE user cache after sign-in.")
              }
    }).describe("Successful sign-in. Returns the bearer access token (RS256 JWT the console stores as `Authorization: Bearer`), the opaque refresh token, the access-token lifetime in seconds, and the authenticated user record. The FE computes `expiresAt = Date.now() + expiresIn * 1000` and persists the triple to localStorage.\n")