{"type":"object","description":"Local-credentials sign-in payload (sigill module). The FE trims + lowercases the email before sending so the backend can compare against the canonical address.","required":["email","password"],"properties":{"email":{"type":"string","format":"email","maxLength":254,"description":"Work email. The local-part is case-insensitive; the backend normalises to lowercase on persist.","example":"demo@plexor.test"},"password":{"type":"string","format":"password","maxLength":128,"minLength":1,"description":"Plaintext password. TLS-only on the wire; never logged.","example":"mock-password"}},"x-readme-ref-name":"PostAuthLoginRequest"}